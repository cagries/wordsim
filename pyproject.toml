[project]
name = "wordsim-pipeline"
version = "1.1.0"
requires-python = ">=3.11"
dependencies = [
  "numpy>=2.0,<3",
  "sentence-transformers==5.7.0",
  "zeyrek==0.1.3",
  "wordfreq>=3.1,<4",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
