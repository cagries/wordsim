{
  "name": "wordsim",
  "version": "1.4.1",
  "private": true,
  "scripts": {
    "build:changelog": "node scripts/build-changelog.mjs",
    "stamp:assets": "node scripts/stamp-assets.mjs",
    "build:game": "npm run stamp:assets && npm run build:changelog && esbuild src/main.ts --bundle --minify --outfile=wordsim/app.js",
    "build:site": "npm run build:game && bundle exec jekyll build",
    "watch:game": "npm run stamp:assets && npm run build:changelog && esbuild src/main.ts --bundle --outfile=wordsim/app.js --watch",
    "test": "esbuild tests/*.test.ts --bundle --platform=node --format=cjs --outdir=.test-build && node --test .test-build/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^18.19.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0"
  }
}
